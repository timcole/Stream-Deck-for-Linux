settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: Stream-Deck-for-Linux
  path: .
  commands:
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
